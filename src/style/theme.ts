import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    mode: 'dark',
    background: {
      default: '#272932',
      paper: '##272932'
    }
  },
  typography: {
    fontFamily: "Tomorrow"
  }
})